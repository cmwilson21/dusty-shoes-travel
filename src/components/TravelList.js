//This is where the travel cards will be displayed and a like button that increments upon click for each place.

import React from 'react';
import TravelCard from './TravelCard'

const TravelList = () => {
  
  return (
    <div>
     <h3>Travel List</h3>
     <TravelCard />
    </div>
  )
}

export default TravelList
